fun main() {
    val userName = "Nestor"
    // magicNum is using an arithmetic magic trick to determine my age
    var magicNum = (2 * 5 + 3) * 2 + 1 - 6
    println("$userName's age is $magicNum.")
}